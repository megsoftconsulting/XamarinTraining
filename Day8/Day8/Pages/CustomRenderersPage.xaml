<?xml version="1.0" encoding="UTF-8"?>
<ContentPage Title="Custom Renderers"
             xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:Day8.Controls"
             BackgroundColor="White"
             x:Class="Day8.Pages.CustomRenderersPage">
    <ContentPage.Content>
        <StackLayout Padding="20" Spacing="15">
            <Label Text="Description" FontAttributes="Bold" FontSize="Subtitle" />
            <Label Text="A Custom Renderer is an implementation of a custom element in Xamarin Forms that can be customized on each platform with their native APIs." 
                   FontSize="Body" />
            <Label Text="A simple sample is to modify the behavior of a Entry:" 
                   FontSize="Body" />
            <Entry Placeholder="Un Entry normal" />

            <local:MyEntry Placeholder="This entry has a color set from the custom renderer." />

            <local:FlatEntry Placeholder="This is a flat enrty" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>
